#include "main.h"
#include <stdio.h>

/**
 * main - checks the code
 *
 * Return: Always 0.
 */

int main(void)
/*Declare and define the variable type and name*/
{
    int n;
/* assigning a value to n*/
    n = _strlen_recursion("Corbin Coleman");
    printf("%d\n", n);
    return (0);
}
